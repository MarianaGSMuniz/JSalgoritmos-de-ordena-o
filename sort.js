const livros = require('./lista-livros.js');
const menorValor = require('./menorvalor');

for (let atual = 0; atual < livros.length; atual++) {
    let = menorValor(livros, atual)

    let livroAtual = livros[atual];

    let livroMenorPreco = livros[menor];

}